
- Goal
A minimal backend microservice to manage books (just title, author).

- Tech Stack

FastAPI → API framework
SQLModel → ORM and schema (Pydantic + SQLAlchemy)
PostgreSQL → Database (via Docker)
Alembic → Migrations